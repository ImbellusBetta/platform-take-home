require 'test_helper'

class DistanceTest < ActiveSupport::TestCase
  # There isn't anything to test for this model. All actions are either apart of the geokit gem (already tested)
  # Or are apart of ActiveRecord (already tested). See LocationTest for unit testing.
end
